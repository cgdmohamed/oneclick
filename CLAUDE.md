# ملاحظات داخلية للمشروع (oneclick / ون كليك)

> **ملحوظة مهمة**: كل ما هو مذكور في هذا الملف هو ملاحظات داخلية للفريق فقط.
> **لا يُذكر أي شيء منها للعميل** إلا لو طلب ميزة بعينها بالتحديد، أو تقرر
> إضافتها فعليًا ويستحسن إخباره استباقيًا. راجع القرار مع صاحب المشروع أولاً.

هذه الملاحظات ناتجة عن مراجعة شاملة لقسم المبيعات (فواتير، إشعارات دائنة/مدينة،
تحصيلات، عملاء) بالمقارنة مع Odoo / QuickBooks / Zoho Books. الأخطاء الحقيقية
الأربعة التي وُجدت في هذه المراجعة تم إصلاحها بالفعل (انظر تاريخ الـ commits).
الباقي أدناه فجوات ميزات وملاحظات معمارية لم تُنفذ بعد.

## ملاحظات معمارية (Technical debt / تحتاج انتباه عند التطوير المستقبلي)

- **مصدر واحد للحقيقة بخصوص رصيد العميل**: تم توحيد حساب `paid`/`remaining` على
  مستوى الفاتورة عبر `recomputeInvoiceBalance()` في
  `artifacts/api-server/src/modules/accounting/posting.ts`. أي كود جديد يعدّل
  حالة الدفع/الإشعارات يجب أن يستخدم هذه الدالة بدلاً من إعادة حساب الرصيد يدويًا،
  لتفادي رجوع نفس فئة الخطأ (تضارب حسابات متعددة).
- كل نوع مستند (فاتورة/إشعار دائن/إشعار مدين) لا يزال يكرر منطق التحقق من
  التوازن الضريبي (VAT-01) بشكل منفصل بدل دالة مشتركة واحدة — مرشح جيد لإعادة
  هيكلة لاحقًا لتقليل احتمال نسيان تحديثه في مكان واحد.

## فجوات ميزات مهمة (موجودة في Odoo/QuickBooks/Zoho ومفقودة هنا)

1. عروض الأسعار وأوامر البيع (Quotation → Sales Order → Invoice) — غير موجودة إطلاقًا.
2. فواتير متكررة (Recurring Invoices) — غير موجودة.
3. قوائم أسعار / تسعير حسب العميل (Price Lists) — كل منتج له سعر واحد فقط.
4. حد ائتماني للعميل (Credit Limit) — لا يوجد أي تحقق أو تنبيه.
5. PDF حقيقي للفاتورة — الطباعة عن طريق طباعة المتصفح فقط، لا يوجد توليد PDF
   فعلي، والإيميل يُرسل كـ HTML بدون مرفق PDF.
6. توزيع دفعة واحدة على أكثر من فاتورة — كل دفعة مربوطة بفاتورة واحدة فقط.
7. تقرير "المبيعات حسب العميل" (Top Customers) — غير موجود (موجود فقط تقرير
   حسب المنتج وكشف حساب العميل).
8. متابعة مندوب المبيعات والعمولات (Salesperson / Commission) — غير موجودة.
9. رصيد ائتمان العميل (Customer Credit Balance عند السداد الزائد) — الدفع
   الزائد عن قيمة الفاتورة ممنوع بالكامل بدل تسجيله كرصيد يُستخدم لاحقًا.

## نقاط تصميم مقصودة (ليست فجوات — قرارات مؤكدة سابقًا)

- عدم دعم العملات المتعددة (EGP فقط) — قرار مقصود وموثق في الكود.
- منع تعديل الفاتورة/الكمية بعد الترحيل — مطابق للممارسات القياسية (Odoo).
- طريقة التكلفة: متوسط مرجّح (Average Cost) بدون FIFO/LIFO — مقبولة محاسبيًا.
- الخصم بقيمة مطلقة (وليس نسبة) على مستوى السطر/الفاتورة.

---

# مراجعة قسم المخزون (نفس المنهجية، بالمقارنة مع Odoo/QuickBooks/Zoho)

الأخطاء الحقيقية الثلاثة التي وُجدت (استيراد CSV بدون قيمة/قيد محاسبي، حركة
"تعديل" بتزوّد المخزون دايمًا، حركة "صادر" بدون تحقق من كفاية الرصيد) تم
إصلاحها بالفعل. الباقي أدناه فجوات ميزات وملاحظات معمارية لم تُنفذ بعد.

## ملاحظات معمارية

- نمط حساب تكلفة الوحدة `COALESCE(NULLIF(average_cost,0), cost, 0)` متكرر
  يدويًا في أكتر من 10 مكان بدل دالة مشتركة — لو الحسبة احتاجت تتغير يوم ما،
  سهل جدًا ينسى تحديثها في مكان.
- `inventory_value` عمود منفصل مش مشتق تلقائيًا من `quantity × average_cost`،
  بيتحدّث يدويًا في كل مسار على حدة — أي مسار جديد (زي الاستيراد) ينساه هيسيب
  القيمة غير متزامنة مع الكمية (فئة الخطأ رقم 1 اللي اتصلحت).
- `applyOpeningStock()` في `artifacts/api-server/src/modules/products/routes.ts`
  هي الآن نقطة الدخول الوحيدة الصحيحة لإعطاء منتج رصيد افتتاحي (تستخدمها شاشة
  "رصيد افتتاحي" واستيراد CSV) — أي مسار جديد يحتاج نفس المنطق يجب أن يستخدمها.

## فجوات ميزات مهمة (موجودة في Odoo/QuickBooks/Zoho ومفقودة هنا)

1. جرد فعلي/عدّ دوري منظم (Physical/Cycle Count) — غير موجود كـ workflow،
   فقط حركة تعديل حرة أو شطب.
2. مخازن/فروع فعلية متعددة — الكمية والقيمة على مستوى الشركة ككل فقط.
3. تتبع أرقام تسلسلية/باتش/تاريخ صلاحية (Serial/Lot/Batch/Expiry) — غير موجود.
4. تحويل وحدات القياس — وحدة واحدة نصية لكل منتج فقط.
5. أمر شراء → استلام بضاعة → فاتورة مورد كخطوات منفصلة — خطوة واحدة بس
   ("فاتورة شراء" مباشرة).
6. تقرير تسوية "قيمة المخزون" مقابل حساب المخزون في الأستاذ العام — موجود
   بالضبط للعملاء والموردين (`reconciliation_difference`) لكن مش للمخزون.
7. تخصيص تكلفة الشحن/الجمارك (Landed Cost) على المشتريات — غير مدعومة.

## نقاط تصميم مقصودة (ليست فجوات)

- منع تعديل الكمية مباشرة، إجبارها تمر بحركة مخزون مدققة — مطابق لأودو.
- المتوسط المرجّح كطريقة تكلفة وحيدة (بدون FIFO/LIFO) — مقبولة محاسبيًا.
- حد أقصى مستويين للتصنيفات — قرار تبسيطي مناسب لمعظم الشركات الصغيرة/المتوسطة.

---

# مراجعة قسم المحاسبة الأساسية (نفس المنهجية، بالمقارنة مع Odoo/QuickBooks/Zoho)

الخطآن الحقيقيان اللي وُجدوا (تعليق القيود المسودة للأبد، إعداد "ملخص الدخل"
بدون أي تأثير) تم إصلاحهم بالفعل. الباقي أدناه فجوات ميزات وملاحظات معمارية
لم تُنفذ بعد.

## ملاحظات معمارية

- `reverseJournalEntry()` في `posting.ts` بيرحّل قيد العكس دايمًا بتاريخ
  *اليوم*، مش تاريخ القيد الأصلي ولا تاريخ يختاره المستخدم — لو الفترة
  الحالية مقفولة، عكس قيد قديم هيتوقف حتى لو الفترة الأصلية مفتوحة.
- سجل النشاط (Audit Log) بيغطي إنشاء/ترحيل/عكس/حذف القيود لكن **مش بيسجل
  تعديل القيد وهو مسودة** (تغيير التاريخ/البيان) ولا تعديل فئات الأصول
  الثابتة أو الأصول نفسها — تغطية غير متسقة تستحق توحيد لاحقًا.

## فجوات ميزات مهمة (موجودة في Odoo/QuickBooks/Zoho ومفقودة هنا)

1. التخلص من الأصول الثابتة (Disposal) — الجدول والإعدادات جاهزة
   (`status='disposed'`, `asset_disposal_gain_loss_account_id`) لكن مفيش
   أي شاشة أو endpoint ينفذها فعليًا.
2. لا يوجد نظام موافقات (Maker-Checker) على القيود — أي مستخدم عنده صلاحية
   "المحاسبة" يقدر ينشئ ويرحّل ويقفل فترات بدون مراجعة من حد تاني.
3. لا يوجد فلترة التقارير المالية بالمشروع (Project) رغم إن البُعد موجود
   فعليًا في القيود.
4. لا يوجد مقارنة فترات في التقارير المالية (هذه السنة/الشهر مقابل السابق).
5. لا يوجد تصدير PDF/Excel حقيقي للتقارير — طباعة المتصفح فقط.
6. الإهلاك يدوي بالكامل بدون جدولة تلقائية دورية.
7. لا يوجد استيراد كشف بنكي حقيقي (OFX/MT940) أو ربط API بنكي — CSV يدوي فقط.

## نقاط تصميم مقصودة (ليست فجوات)

- عدم دعم العملات المتعددة على مستوى دليل الحسابات والقيود بالكامل.
- إقفال السنة المالية بترحيل مباشر لحساب الأرباح المرحّلة (بدون حساب وسيط) —
  مقبولة ومطابقة لـQuickBooks.
- الإهلاك بطريقة القسط الثابت فقط (بدون طرق تناقصية) — مقبولة للأنظمة البسيطة.

---

# مراجعة قسم العمليات والتقارير (نفس المنهجية، بالمقارنة مع Odoo/QuickBooks/Zoho)

الأخطاء الحقيقية الثلاثة اللي وُجدوا (تجاهل `recomputeInvoiceBalance()` لإعدام
الديون، إخفاء جدول تفصيل الـVAT + كراش عند تحميل الصفحة، حالة "مدفوعة" مضللة
لفاتورة معدومة) تم إصلاحهم بالفعل. مؤشر "معدومة" الجديد مُضاف كـ badge بصري فقط
(`has_bad_debt_writeoff`) بدون أي تغيير في enum حالة الفاتورة أو منطق حسابها —
قرار متعمد لتفادي المساس بأي منطق تم إصلاحه في المراجعات السابقة.

## ملاحظات معمارية

- **مهم**: أي كود جديد يخصم/يضيف لرصيد فاتورة (زي إعدام الديون الآن) **لازم**
  يستخدم `recomputeInvoiceBalance()` في `posting.ts` ولازم الدالة نفسها تتحدّث
  لو ظهر مصدر جديد يؤثر على رصيد الفاتورة — الخطأ رقم 1 في هذه المراجعة كان
  بالظبط لأن الدالة اتعملت قبل ما إعدام الديون يتفحص.
- ~~صفحة `FinancialReportDetail.tsx` بها شرط دائم الصحة (tautology)~~ — تم
  التنظيف (شريط الفلاتر بيتعرض دايمًا بشكل مباشر بدون شرط ميت).

## فجوات ميزات مهمة (موجودة في Odoo/QuickBooks/Zoho ومفقودة هنا)

الفجوات 1-4 تم إغلاقها بناءً على طلب صاحب المشروع (رآها منطقية):

1. ~~لا يوجد مسار "استرداد" منفصل عن "عكس الإعدام"~~ — تمت إضافة زر "استرداد" في
   `BadDebts.tsx` لأي إعدام مرتبط بفاتورة: يعكس الإعدام بالكامل (يستخدم
   `recomputeInvoiceBalance()` عبر مسار الإلغاء الموجود) ثم يسجل دفعة تحصيل فعلية
   بالمبلغ المُدخل — أي جزء غير مُسترد يرجع مستحقًا على العميل من جديد بدل إعدامه.
   الإعدامات العامة (بدون فاتورة) لسه بيستخدموا "عكس" فقط.
2. ~~قائمة التقارير مسطحة~~ — `FinancialReports.tsx` بقت مقسّمة لـ5 مجموعات
   (قوائم مالية/دفاتر أستاذ/ضرائب/مخزون وأصول/رواتب) + قسم "شوهدت مؤخرًا"
   (localStorage فقط، `recentReports.ts`، مش بيانات عمل حرجة). المفضّلة والجدولة
   الدورية بالإيميل لسه غير موجودين (الجدولة تحتاج بنية Jobs خلفية، اتأجّلت عمدًا).
3. ~~المطابقة يدوية بالكامل~~ — تم ربط endpoint `/suggestions` الموجود مسبقًا في
   الباك إند بواجهة `BankReconciliationDetails.tsx`: عند اختيار بند كشف بنك،
   تظهر قيود اليومية غير المطابقة بنفس المبلغ (مع مراعاة انعكاس مدين/دائن) كاقتراح
   أول بدل تصفح كل القيود يدويًا.
4. ~~استيراد CSV بلصق نص فقط~~ — تمت إضافة رفع ملف حقيقي (`<input type="file">`
   + `FileReader`) مع إبقاء اللصق اليدوي كخيار احتياطي قابل للطي. تم أيضًا توسيع
   اكتشاف صف العناوين التلقائي ليتعرف على عناوين عربية (تاريخ/وصف/مدين/دائن/مرجع)
   مش بس إنجليزية — كان فيه خطأ حقيقي هنا: كشف بعناوين عربية كان بيتفسر كصف بيانات
   ويفشل الاستيراد بالكامل (تم اكتشافه وإصلاحه أثناء الاختبار الحي لنفس الميزة).

5. تقرير الـVAT مش جاهز للتقديم الرسمي لمصلحة الضرائب المصرية (ETA) — أرقام
   عامة بس بدون ربط بفترة إقرار ضريبي. **لم تُنفذ عمدًا**: تحتاج متطلبات دقيقة
   لشكل ملف/تقرير مصلحة الضرائب المصرية (ETA) غير متوفرة حاليًا؛ تنفيذها بدون
   المتطلبات الحقيقية هيطلع تقرير شكله رسمي لكنه غير مطابق فعليًا، وده أخطر من
   عدم وجوده. يحتاج قرار ومتطلبات من صاحب المشروع أولاً.

## نقاط تصميم مقصودة (ليست فجوات)

- طريقة إعدام الدين "غير مباشرة" (خصم من حساب مخصص مُكوَّن مسبقًا) — مطابقة
  للمعيار المحاسبي المتعارف عليه (Allowance Method).

---

# مراجعة قسم الأصول الثابتة والرواتب (نفس المنهجية، بالمقارنة مع Odoo/QuickBooks/Zoho)

الخطآن الحقيقيان اللي وُجدوا تم إصلاحهم بالفعل:

1. ~~`PATCH /fixed-assets/assets/:id` كان بيسمح بتغيير `status` مباشرة~~ — ده كان
   بيسمح بوضع أصل كـ`disposed` أو `fully_depreciated` بدون أي قيد تخلص أو تحقق
   من اكتمال الإهلاك فعليًا (`asset_disposal_gain_loss_account_id` مش مستخدم في
   الملف كله أصلاً). تم سد الثغرة: أي محاولة لتمرير `status` في الـPATCH العام
   بترفض دلوقتي بـ409 — الحالة بتتغير فقط تلقائيًا جوه `postRun()` (لما الإهلاك
   يكتمل). مفيش شاشة أمامية كانت بتستخدم الثغرة دي أصلاً، فالإصلاح باك إند بحت
   بدون أي أثر على الواجهة.
2. ~~لا يوجد مسار لإلغاء/عكس تشغيل رواتب بعد الترحيل أو الدفع~~ — تمت إضافة
   `POST /payroll/runs/:id/cancel` بنفس نمط `/depreciation-runs/:id/cancel`
   الموجود مسبقًا: بيعكس قيد الدفع (لو موجود) ثم قيد الترحيل عبر
   `reverseJournalEntry()`، وبيرجّع رصيد حساب الدفع لو كان اتخصم فعليًا،
   ويحوّل الحالة لـ`cancelled` (القيمة كانت موجودة في الـenum من الأساس بدون أي
   طريق ليها). تم اختباره حيًا: تشغيل → ترحيل → دفع (خصم فعلي من رصيد حساب) →
   إلغاء → الرصيد رجع بالظبط لقيمته قبل الدفع، وحالة القيدين بقت `reversed`.
   زرار "إلغاء التشغيل" اتضاف في `PayrollRunDetails.tsx` لما الحالة `posted` أو
   `paid` (بتأكيد قبل التنفيذ لأنه بيعكس أموال حقيقية).

## ملاحظات معمارية

- تكرار نمط الترحيل (قفل الصف + فحص idempotency + بناء قيد + تحديث السجل) بين
  `postRun` (الأصول) و`postPayroll` (الرواتب) بدون دالة مشتركة — boilerplate
  مكرر (مش نفس القاعدة المحاسبية) لكن مرشح لتوحيد لاحق.
- حسابات فئة الأصل اختيارية مع fallback على إعدادات الشركة؛ لو الاتنين فاضيين
  رسالة الخطأ اللي بترجع ("Journal line account does not belong to company")
  مش واضحة إن السبب حساب إهلاك غير مُهيأ — تحسين تجربة استخدام مش خطأ منطقي.

## فجوات ميزات مهمة (موجودة في Odoo/QuickBooks/Zoho ومفقودة هنا)

### الأصول الثابتة
1. لا يوجد endpoint فعلي للتخلص من الأصول (Disposal) رغم جهوزية الجدول
   والإعدادات (`status='disposed'`, `asset_disposal_gain_loss_account_id`).
2. لا يوجد تحويل أصل بين فروع/مراكز تكلفة كعملية مدققة ومسجلة تاريخيًا.
3. لا يوجد Partial Disposal ولا Asset Revaluation.
4. الإهلاك بقسط ثابت شهري بدون proration زمني — لو اتخطت فترة، مفيش تعويض
   تلقائي لاحق.

### الرواتب
5. لا يوجد حساب تلقائي للضرائب أو التأمينات الاجتماعية — كل المكونات مبالغ
   حرة تُدخل يدويًا (مذكور صراحة في نص الواجهة كقرار مقصود، انظر أدناه).
6. لا يوجد هيكل راتب/عقد دائم للموظف — كل شهر بيتعاد إدخال كل مكون من الصفر.
7. لا يوجد كشف راتب (Payslip) مطبوع/PDF لكل موظف.
8. لا يوجد سُلف/قروض موظفين تُخصم تلقائيًا من رواتب لاحقة.
9. لا توجد مكافأة نهاية خدمة عند تغيير حالة الموظف لغير نشط.
10. لا يوجد ربط بالحضور والانصراف أو الإجازات (خصم غياب تلقائي، أوفرتايم).
11. تقارير الرواتب الموجودة (`summary`/`by-employee`/`by-dimension`) داخلية
    فقط، مش بصيغة نماذج التأمينات/الضرائب المصرية الرسمية — نفس فئة ملاحظة
    تقرير الـVAT، تحتاج قرار ومتطلبات رسمية من صاحب المشروع.

## نقاط تصميم مقصودة (ليست فجوات)

- منع تعديل التكلفة/العمر الافتراضي/تاريخ بداية الإهلاك بعد وجود إهلاك مُرحّل —
  مطابق لمنع تعديل الفواتير المرحّلة في باقي النظام.
- قيد Unique لكل (شركة، فترة) في `depreciation_runs` و`payroll_runs` (باستثناء
  الملغاة) — يمنع تشغيل مزدوج لنفس الفترة، تصميم سليم ومتسق بين الوحدتين.
- الفصل بين `post` (التزام محاسبي) و`pay` (سداد فعلي) في الرواتب — مطابق تمامًا
  لنمط Odoo/QuickBooks (Accrual ثم Payment).
- عدم وجود نسب تأمينات/ضرائب ثابتة مبرمجة، والاعتماد الكامل على مكونات يدوية —
  قرار مُعلن صراحة في نص الواجهة نفسها، على الأرجح لتفادي التعقيد التشريعي
  المصري المتغير باستمرار (نفس تحفظ الفريق حول عدم بناء تقرير VAT/تأمينات رسمي
  بدون متطلبات دقيقة).

---

# مراجعة قسم الإعدادات والإشعارات والملف الشخصي

بدأت بشكوى مباشرة من المستخدم: زر التنبيهات على الموبايل بيملأ الشاشة والمستخدم
مبيقدرش يتحرك. تم تشخيصها وإصلاحها فورًا (انظر أدناه)، وبعدين استكملت مراجعة
شاملة للإعدادات/الإشعارات/الملف الشخصي بنفس المنهجية.

## أخطاء حقيقية

1. ~~جرس التنبيهات كان زر تنقّل كامل الصفحة بدون أي لوحة/بوب-أوفر ولا زر
   رجوع~~ — **تم الإصلاح**. الجرس كان `onClick={() => navigate('/app/notifications')}`
   مباشرة (`AppLayout.tsx`)، وده كان بياخد المستخدم لصفحة كاملة الشاشة، وعلى
   الموبايل الطريقة الوحيدة للخروج منها هي فتح القائمة الجانبية اللي بتخفي زر
   الإغلاق X الافتراضي عمدًا (`sidebar.tsx`: `[&>button]:hidden`) — فمفيش أي
   مخرج واضح، وده اللي حسّه المستخدم كـ"الشاشة بتملي ومبقدرش أتحرك". تم تحويل
   الجرس لـ`Popover` حقيقي (`w-[calc(100vw-2rem)] sm:w-96 max-h-[70vh]
   overflow-y-auto`) بيعرض آخر 6 تنبيهات + زر "تعليم الكل كمقروء" + رابط "عرض
   كل التنبيهات"، وبيتقفل بضغطة برّاه أو Esc عادي زي أي Popover. أضفت
   `useNotificationsPreview()` في `useNotificationsAlerts.ts` لجلب نسخة خفيفة
   منفصلة (6 عناصر بس) عشان فتح الجرس يفضل سريع بغض النظر عن عدد التنبيهات.
   تم اختباره حيًا على viewport موبايل (390×844) وديسكتوب: اللوحة صغيرة
   ومحصورة، الصفحة اللي وراها فاضلة ظاهرة وقابلة للتفاعل، وتعليم كمقروء
   بيحدّث العداد فورًا في الاتنين.
2. ~~لوحة "تنبيهات الفواتير" في الإعدادات كانت واجهة بلا باك إند~~ — **تم
   بناء الميزة فعليًا بالكامل**: جدول `invoice_alert_settings` (jsonb لكل
   شركة) بدل `localStorage` وحده (لسه بيتخزن محليًا كـcache فوري بس مصدر
   الحقيقة بقى السيرفر عبر `GET/PUT /api/companies/invoice-alert-settings`)،
   + مهمة مجدولة جديدة `tickInvoiceAlerts()` في `jobs/invoiceAlerts.ts`
   بتشتغل كل ساعة (مربوطة بعد `tickOverdue()` مباشرة عشان الفواتير اللي
   اتعلّمت "متأخرة" في نفس الدورة تتفحص فورًا مش دورة لاحقة). الميزة بتغطي
   فعليًا: `onDueSoon` (فاتورة قربت تستحق)، `onOverdue` (متأخرة، بتتكرر كل
   `repeatEveryDays`)، `onPaid` (اتحصّلت) — بتحترم الجمهور المستهدف
   (عميل/مستخدمين/الكل) والقنوات (بريد فعلي عبر `sendEmail`/`renderEmail`
   الموجودين أصلاً + إشعار داخل التطبيق)، وساعات الهدوء، ووضع الجدولة
   (فوري/يومي/أسبوعي عبر تحديد الساعة المسموح فيها التشغيل). جدول
   `invoice_alert_log` بيمنع تكرار نفس التنبيه لنفس الفاتورة (اتأكد بالاختبار
   الحي: نفس الفاتورة اتفحصت مرتين متتاليتين وبعتت مرة واحدة بس). التنبيهات
   الآلية بتتسجل بنفس شكل `kind='invoice_email'` اللي بيستخدمه إرسال البريد
   اليدوي بالظبط، فبتظهر تلقائيًا في تبويب "تنبيهات الفواتير المُرسَلة"
   الموجود من غير أي تعديل إضافي في الواجهة — اتأكد بالاختبار الحي (سكرين شوت).
   حد "onCreated" (عند إصدار الفاتورة) لسه بيتبعت يدويًا بس زي ما كان (مفيهوش
   لبس محاسبي، مجرد إرسال بريد الفاتورة الحالي).
3. ~~تغيير كلمة المرور مبيبطلش الجلسات التانية~~ — **تم الإصلاح**. `POST
   /api/auth/change-password` بقى بيبطل كل الـ refresh tokens التانية لنفس
   المستخدم (زي `confirm-email-change` بالظبط)، مع استثناء الجلسة الحالية
   نفسها (بنفس منطق `DELETE /sessions?others=true` الموجود مسبقًا) عشان
   المستخدم نفسه ميتسجّلش خروج من شاشته وهو بس غيّر كلمة السر بتاعته. الواجهة
   بقت بتوضح ده صراحة قبل وبعد الضغط. اتأكد بالاختبار الحي: جلستين منفصلتين،
   تغيير كلمة المرور من الجلسة الأولى، الجلسة التانية بقت refresh token
   بتاعها مرفوض ("Refresh token reuse detected")، والجلسة الأولى فضلت شغالة
   عادي.

## فجوات ميزات مهمة

- لا يوجد سجل تدقيق (Audit Log) لتغييرات إعدادات الشركة (`PATCH /companies/me`,
  SMTP settings) — بعكس وحدات تانية زي `users/routes.ts` بتسجل كل تغيير.
- لا يوجد 2FA/MFA.
- الأدوار ثابتة ومحدودة (4 أدوار بس) بدون صلاحيات مخصصة لكل شاشة/إجراء.
- تغيير البريد الإلكتروني لا يطلب كلمة المرور الحالية كخطوة تأكيد إضافية.
- لا يمكن الضغط على عنصر تنبيه للانتقال مباشرة للمستند المرتبط (فاتورة/دفعة).
- لا توجد تفضيلات إشعار حقيقية لكل مستخدم (تفعيل/تعطيل كل نوع تنبيه).
- لا يوجد بث لحظي (WebSocket/SSE) للتنبيهات — Polling كل 60 ثانية بس (مقبول
  لحجم التطبيق الحالي).
- لا يوجد رفع صورة شخصية (Avatar) للمستخدم رغم وجود رفع شعار كامل للشركة.
- `POST /api/notifications` (إشعار يدوي) بدون تحقق من صلاحية الدور — أي
  مستخدم عادي يقدر يبعت إشعار لبقية مستخدمي شركته (فجوة صلاحيات بسيطة، تأثير
  محدود لأنها داخلية بحتة بلا أثر مالي).

## ملاحظات معمارية

- جدول `notifications` الواحد بيتستخدم لغرضين مختلفين (تنبيهات نظام + سجل
  تدقيق بريد الفواتير المُرسَل بـ`kind='invoice_email'`) بدون دالة إنشاء
  مشتركة واحدة (3 أماكن منفصلة بتضيف مباشرة في الجدول).
- تناقض نمط الحفظ داخل نفس صفحة الإعدادات: تبويبات بتحفظ تلقائيًا (debounce)
  وتبويب SMTP محتاج زر "حفظ" صريح.

## نقاط تصميم مقصودة

- فصل `kind='invoice_email'` عن عدّاد "غير مقروء" في الجرس حتى لا تُحتسب
  سجلات التدقيق كتنبيهات مزعجة — قرار سليم.
- إدارة الجلسات النشطة (`AccountSettings.tsx`) منفَّذة بجودة جيدة فعليًا
  (متصفح/جهاز/IP مُقنَّع/تمييز الجلسة الحالية) — استثناء إيجابي.
- عدم وجود تفضيل لغة/منطقة زمنية — مقبول حاليًا (سوق مصري فقط)، يستحق إعادة
  نظر لو خطة التوسع تغيرت مستقبلًا.

---

# إصلاح: حساب بنكي جديد بكود فرعي (1021) ما بيظهرش في كشف الحساب

بلاغ مباشر من المستخدم. السبب: في النظام جدولين منفصلين للحسابات —
`accounts` (خزائن/حسابات بنكية/محافظ، تستخدمها شاشة "الحسابات المالية"
`Accounts.tsx`، ملهاش عمود كود إطلاقًا) و`chart_accounts` (دليل الحسابات
المحاسبي، وده اللي "كشف حساب" بيقرا منه فقط). لما المستخدم يضيف حساب بنكي
جديد من "الحسابات المالية" مفيش أي ربط تلقائي بينه وبين دليل الحسابات، فكل
الحسابات البنكية بتترحّل فعليًا على نفس حساب "Bank" الافتراضي المشترك
(`accounting_settings.bank_account_id`) — مفيش طريقة ليدّي حساب بنكي معيّن
كود مستقل يظهر بيه في كشف الحساب.

**الخبر الكويس**: الباك إند فعليًا بيدعم الربط من الأساس — `moneyAccountChartAccount()`
في `posting.ts` بتفضّل عمود `accounts.chart_account_id` لو موجود قبل ما ترجع
للحساب الافتراضي المشترك. المشكلة كانت بس إن العمود ده مش متاح في الـ API
ولا الواجهة (`accounts/routes.ts` ماكانش فيه `chart_account_id` في schema
ولا `fields`، و`Accounts.tsx` مفيهاش أي حقل لاختياره).

**تم الإصلاح**: أضفت `chart_account_id` لـschema/fields في `accounts/routes.ts`
(مع تحقق إنه ينتمي لنفس الشركة)، وأضفت حقل "الحساب في دليل الحسابات (اختياري)"
في نموذج `Accounts.tsx` بيسمح للمستخدم يربط أي حساب بنكي/خزنة/محفظة بكود
مستقل من دليل الحسابات (زي 1021) بدل الاعتماد على الحساب الافتراضي المشترك.
الحسابات القديمة اللي مالهاش ربط بتفضل شغالة زي ما هي بالظبط (fallback
للحساب المشترك) — الإصلاح إضافي بالكامل ومفيهوش أي تغيير في سلوك محاسبي قائم.

اتأكد بالاختبار الحي الكامل: إنشاء حساب `1021` في دليل الحسابات → ربط حساب
بنكي جديد بيه → تسجيل دفعة عليه → القيد اترحّل فعليًا على `1021` (مش الحساب
المشترك 1020) → ظهر بشكل صحيح في "كشف حساب" بالرصيد والقيد الصح، وفي شاشة
"الحسابات المالية" بيظهر "دليل الحسابات: 1021" تحت اسم الحساب.

**ملاحظة مهمة اكتشفناها بعد الإصلاح**: ربط حساب **موجود بالفعل وعليه معاملات
قديمة** (زي "البنك الأهلي - جاري" اللي كان عليه 650 ج.م متحصّلة قبل الربط)
بكود جديد **لا يرجّع بالزمن وينقل القيود القديمة** — دي فضلت مرحّلة على الكود
المشترك القديم (1020)، وبس المعاملات الجديدة من لحظة الربط وطالع هي اللي
هتظهر تحت الكود الجديد. ده سلوك متعمد (مفيش أي كود بيغيّر `account_id` لقيود
مرحّلة بالفعل، وده صح محاسبيًا — تعديل قيد مرحّل من غير عكس رسمي مش ممارسة
سليمة). **قرار صاحب المشروع**: بما إن مفيش مستخدمين حقيقيين على النظام لسه،
اتفقنا نكتفي بالسلوك الحالي (من لحظة الربط وطالع بس) من غير أي نقل/تسوية
للقيود القديمة — الأمر يستأهل مراجعة تانية لو النظام دخل مرحلة إنتاج فعلية
وعندها حسابات بمعاملات تاريخية محتاجة تتربط بكود مستقل.

---

# إضافة: تجميع (rollup) الحسابات الفرعية تحت الحساب الأب في كل التقارير المالية

بعد إصلاح ربط الحساب البنكي، المستخدم لاحظ إن حساب 1021 (الفرعي) بيظهر صح،
لكن **حساب 1020 (الأب) مبيظهرش فيه معاملات 1021 لما تختاره في التقارير** —
اتضح إن مفيش أي منطق تجميع هرمي للحسابات في أي تقرير مالي خالص (لا كشف
حساب، ولا ميزان مراجعة، ولا قائمة الدخل، ولا الميزانية) — كل تقرير كان بياخد
رصيد كل حساب من قيوده المباشرة بس (`WHERE jel.account_id = ca.id`)، من غير
أي اعتبار للعلاقة `parent_id` الموجودة أصلاً في `chart_accounts`. طلب صاحب
المشروع نطبّقها على كل الحسابات/كل التقارير (زي مفهوم "Sub-account" في
QuickBooks: اختيار الحساب الأب في أي تقرير يوريك رصيده + كل حساباته الفرعية
مجمّعين).

**التنفيذ**: أضفت `ACCOUNT_ROLLUP_CTE` (recursive CTE واحد مشترك) في
`reports/routes.ts` بيوسّع كل حساب لقائمة (نفسه + كل أحفاده)، واستخدمته في:
- `account-statement` (الاتنين variants): بدل `jel.account_id = $accountId`
  بقت `jel.account_id IN (SELECT descendant_id ... WHERE ancestor_id = $accountId)`
  — يعني كشف حساب الأب دلوقتي بيعرض قيوده هو + قيود كل حساباته الفرعية في
  قائمة واحدة مرتبة بالتاريخ برصيد جاري تراكمي صحيح.
- `trial-balance`, `income-statement`, `balance-sheet`: كل صف حساب بقى
  رصيده = رصيده المباشر + رصيد كل أحفاده (rollup)، **لكن** إجماليات
  التقرير (مجموع مدين/دائن، صافي الأصول/الالتزامات/حقوق الملكية، صافي
  الدخل) بتتحسب بس من الحسابات الجذرية (`parent_id IS NULL`) لتفادي حساب
  نفس المبلغ مرتين (مرة في صف الحساب الفرعي، ومرة تانية داخل رصيد أبوه
  المجمّع) — رياضيًا مجموع كل الحسابات الجذرية المجمّعة = مجموع كل الحسابات
  الفرعية منفردة، فمفيش أي فرق في الإجمالي الحقيقي، بس اتجنبنا الحساب المزدوج.

اتأكد بالاختبار الحي: اختيار 1020 (الأب) في كشف حساب بيوري الـ4 قيود (2
بتاعته + 2 بتاعة 1021) برصيد تراكمي صح لحد 1,650. ميزان المراجعة: صف 1020
بيوري 1,650 (رصيد مجمّع)، صف 1021 بيوري 1,000 (رصيده هو بس، مفيهوش أحفاد)،
وإجمالي مدين = إجمالي دائن = 14,379.48 (متوازن). الميزانية: نفس الشيء،
`is_balanced: true` والفرق صفر.

---

# اختبار ميداني بـ4 وكلاء (محاسب سوبر ماركت/محل أحذية/مكتبة/شركة أعلاف)

طلب صاحب المشروع تشغيل 4 وكلاء مستقلين، كل واحد بشخصية محاسب حقيقي في نشاط
مختلف، كل واحد بحساب شركة منفصل، لاختبار النظام حيًا (مش مراجعة كود) والإفادة
بتقرير. التقرير الكامل اتسلّم للمستخدم كـWord doc. أهم اللي طلع:

**أخطاء حقيقية جديدة (لسه مش متصلحة، تحتاج فريق التطوير):**
1. حقول السعر/التكلفة/الكمية في نموذج "منتج جديد" (وكمان حقل اسم العميل في
   "عميل جديد") مش مربوطة بوسم `<label>` سليم (لا `id`/`for` ولا
   `aria-label`) — ده مش بس مشكلة accessibility، إنه فعليًا سبب حفظ منتجات
   بسعر/تكلفة صفر بصمت لمحاسبَين مختلفين في اختبارين منفصلين (سوبر ماركت
   ومحل أحذية)، واحتمال حفظ عميل باسم فاضي بدون أي رسالة خطأ.
2. حركة "وارد" اليدوية في المخزون (`stock-movements`) بتحدّث الكمية والقيمة
   الإجمالية صح، لكن **مبتحدّثش `average_cost`/`cost`** — على عكس فاتورة
   الشراء اللي بتطبّق صيغة المتوسط المرجّح صح. الأثر: أي بيع بعد حركة وارد
   يدوية هيحسب تكلفة بضاعة مباعة أقل من الحقيقية (نفس فئة الخطأ المذكورة في
   ملاحظة "نمط حساب تكلفة الوحدة متكرر يدويًا" في مراجعة المخزون أعلاه).
3. منتج نوعه "خدمة" لسه بيعرض حقول مخزون كاملة (كمية/حد تنبيه/حسابات مخزون
   وتكلفة بضاعة) وبيولّد تنبيه "قارب على النفاد" وهمي رغم إنه بلا مخزون
   أصلاً.
4. نسبة الضريبة الافتراضية على منتج جديد (14%) مش بتتقرأ من نفس إعداد ضريبة
   الشركة الفعلي (كان 15% في نفس الاختبار) — نفس الإصلاح اللي اتعمل لشاشة
   الفاتورة (أعلاه) يبدو إنه ماتطبّقش على نموذج إنشاء منتج.
5. تسريب تسميات إنجليزية خام في تقريري "أعمار الديون" و"الميزانية" (زي
   `days 61 90`, `is balanced`, `current year start`, أسماء حسابات
   افتراضية `Cash`/`Bank`/`Wallet` بالإنجليزي) وسط واجهة معرّبة بالكامل.
6. مفيش حساب مالي (خزينة/بنك) بيتنشئ تلقائيًا لشركة جديدة — أول محاولة
   تحصيل بتوقف لحد ما المستخدم يعمل حساب يدويًا.
7. منتقي المنتج في شاشة "شطب المخزون" `<select>` عادي بدون بحث، وحالة
   "posted" بتظهر بالإنجليزي — تفاصيل تجربة استخدام بسيطة.

**فجوات نطاق تأكدت حيًا من منظور كل نشاط (مش أخطاء، قرارات تحتاج مناقشة):**
- **محل أحذية**: مفيش مفهوم "متغيّر منتج" (مقاس/لون) خالص — لازم منتج
  منفصل لكل مقاس. ومفيش مسار "استبدال" مخصص (بيعتمد على إشعار دائن + فاتورة
  بيع منفصلة يدويًا بدون رابط بينهم).
- **مكتبة/عملاء آجلين مؤسسيين**: مفيش حد ائتماني، مفيش قوائم أسعار للبيع
  بالجملة، مفيش PDF حقيقي للفاتورة، مفيش توزيع دفعة واحدة على أكتر من فاتورة.
- **تجارة الأعلاف بالجملة**: مفيش تتبع تاريخ صلاحية/باتش خالص، ومفيش حد
  ائتماني للعميل (اتأكد حيًا: تكديس فاتورتين آجلتين بـ~148,200 ج.م على نفس
  العميل من غير أي تحذير). في المقابل: تقرير أعمار الديون وموديول شطب
  المخزون التالف بيشتغلوا صح 100% (اتأكد بقيود محاسبية فعلية).

**نقطة إيجابية مشتركة عبر كل الأنشطة الأربعة**: المحرك المحاسبي نفسه (حساب
الضريبة لكل بند، توزيع الخصومات التناسبي، المتوسط المرجّح، ترحيل القيود،
توازن ميزان المراجعة/الميزانية) طلع دقيق 100% في كل اختبار حي، حتى مع فواتير
كبيرة (100,000+ ج.م) وعمليات مركّبة (خصم + ضريبة + شطب + تحصيل جزئي) — كل
الأرقام اتراجعت يدويًا وطابقت تمامًا.

---

# إصلاح: حقل ضريبة مزدوج في شاشة "فاتورة جديدة" كان بيربك المستخدم

بلاغ مباشر من المستخدم: عند إنشاء فاتورة، فيه حقل ضريبة على كل بند + حقل
ضريبة تاني منفصل على مستوى الفاتورة، ومش واضح أنهم بيطبّق فعليًا.

**السبب**: الحقلين كانوا بيعملوا نفس الحاجة بالظبط (نسبة ضريبة تُطبَّق على
البند)، مش غرضين مختلفين. حقل مستوى الفاتورة ("نسبة الضريبة %") كان مجرد
"مصنع دفعة واحدة" بيكتب فوق نسبة كل البنود في نفس اللحظة اللي تتغيّر فيها
قيمته — يعني لو المستخدم عدّل نسبة بند بعينه، ثم رجع لمس حقل الفاتورة تاني
ولو بالخطأ، كل تعديلاته اليدوية كانت بتتمسح فورًا بدون تأكيد. الحقل ده
أصلاً **مش بيتبعت للسيرفر خالص** — بس نسبة كل بند على حدة هي اللي بتترحّل
فعليًا وتدخل في حساب الإجمالي، وده اللي كان بيسبب اللبس ("أنهي حقل بيطبّق
فعليًا؟").

**تم الإصلاح**: شيلت حقل "نسبة الضريبة %" على مستوى الفاتورة بالكامل، وبقى
في حقل ضريبة واحد بس لكل بند. النسبة الافتراضية لأي بند جديد بقت بتتحدد
تلقائيًا من: (١) نسبة الضريبة الخاصة بالمنتج نفسه لو موجودة (شاشة المنتج،
موجودة أصلاً)، وإلا (٢) نسبة الضريبة الافتراضية المسجّلة في إعدادات الشركة
(الإعدادات ← الفاتورة ← "نسبة الضريبة %") بدل رقم ثابت (١٥٪) كان مكتوب في
الكود. اتأكد بالاختبار الحي: غيّرت نسبة ضريبة الشركة لـ١٤٪، وفتحت فاتورة
جديدة، ولقيت حقل الضريبة الوحيد المتبقي (على البند) اتملى تلقائيًا بـ١٤
بدل ١٥ القديمة، والحقل المزدوج اختفى تمامًا من الشاشة.

---

# تحسينات: دمج تكرار المنتج في الفاتورة + إلغاء "إضافة منتج جديد" من شاشة الفاتورة

طلب مباشر من صاحب المشروع، اتنفذ الاتنين في `NewInvoice.tsx`:

1. **اختيار نفس المنتج في بند تاني بقى بيحدّث الكمية بدل ما يضيف بند مكرر** —
   لو المستخدم اختار منتج موجود بالفعل في بند سابق بنفس الفاتورة، النظام
   بيزوّد كمية البند الموجود بكمية البند الجديد (افتراضيًا ١) ويحذف البند
   المكرر تلقائيًا، مع رسالة تأكيد ("تم تحديث الكمية في بند «...» الموجود
   بدل تكراره"). اتأكد بالاختبار الحي: اختيار نفس المنتج في بندين، النتيجة
   بند واحد بكمية ٢ والإجمالي محدَّث صح.
2. **إلغاء زرار "إضافة منتج جديد" (الاختصار السريع) من شاشة الفاتورة بالكامل** —
   كان بيفتح نموذج مصغّر لإنشاء منتج بحقول ناقصة (بدون تصنيف كامل، ضريبة،
   بيانات محاسبية) من قلب شاشة الفاتورة، وده مش منطقي لأن إنشاء منتج محتاج
   قيد إدخال أساسي كامل من شاشة "المنتجات" المخصصة له. تم حذف الزرار والنموذج
   المصغّر وكل منطق اكتشاف التكرار المرتبط بيه بالكامل — إنشاء منتج جديد
   بقى ممكن بس من شاشة المنتجات نفسها.

---

# إضافة: بحث داخل القوائم المنسدلة الكبيرة (Searchable Dropdowns)

طلب صاحب المشروع: "أي dropdown متوقع فيه بيانات كتيرة خلي فيه بحث" — القوائم
المنسدلة العادية (`<Select>`) بتاعة العملاء/الموردين/المنتجات/دليل الحسابات
كانت بتعرض كل الصفوف بدون أي فلترة، وده هيبقى غير عملي بمجرد ما عدد
العملاء/المنتجات يكبر (تمرير يدوي طويل بدون بحث).

**التنفيذ**: أضفت مكوّن مشترك جديد `SearchableSelect.tsx` (Popover + Command
combobox، نفس نمط منتقي المنتج الموجود مسبقًا في شاشة الفاتورة) واستبدلت بيه
كل `<Select>` مربوط بقائمة متوقع نموها لتصبح كبيرة:

- شاشة فاتورة جديدة (`NewInvoice.tsx`): منتقي العميل.
- الإشعارات الدائنة/المدينة (`NewCreditNote.tsx`, `NewDebitNote.tsx`):
  منتقي العميل والمنتج.
- إعدام الديون (`BadDebts.tsx`): منتقي العميل (في نموذجي الإعدام العام
  والمباشر).
- المدفوعات للموردين (`Payouts.tsx`): منتقي حساب الخصم من دليل الحسابات،
  ومنتقي المورد.
- المنتجات (`Products.tsx`): منتقي الحساب المحاسبي المشترك (يُستخدم 12 مرة
  في تبويب الحسابات وربط التصنيفات)، منتقي المورد، ومنتقيي المنتج/المورد في
  حركة المخزون.
- التحصيلات (`Payments.tsx`): منتقي حساب القيد الدائن.
- دليل الحسابات (`ChartOfAccounts.tsx`): منتقي الحساب الأب (ذاتي المرجعية).
- الحسابات المالية (`Accounts.tsx`): منتقي الحساب المرتبط من دليل الحسابات
  (نفس الحقل المُضاف في إصلاح حساب 1021 أعلاه).

القوائم اللي اتسيبت عمدًا من غير تحويل (حجم بيانات محدود بطبيعته): فئات
المصروفات، تصنيفات المنتجات، ومنتقي الفاتورة المفتوحة في شاشة التحصيلات —
دي مرشحة لاحقًا لو حجمها كبر فعليًا.

اتأكد بالاختبار الحي (Playwright) على شاشة الفاتورة الجديدة: فتح القائمة
بيظهر مربع بحث، كتابة جزء من اسم العميل بيفلتر النتائج فورًا لعنصر واحد
مطابق، والاختيار بيحدّث الحقل. أثناء الاختبار ظهر خطآن بيئيان مش لهم علاقة
بالتعديل نفسه: جولة onboarding التلقائية بتفتح على كل صفحة جديدة وتعمل
overlay شفاف بيمنع الكليك حتى لو مغطاة بصريًا بنافذة تانية (لازم "تخطي
الجولة" صريح مش Escape بس)، ومحدد معدل تسجيل الدخول (`authLimiter`, 30
محاولة/15 دقيقة) بيقفل بعد تكرار اختبارات تسجيل الدخول — الاتنين سلوك متوقع
في البيئة، مش خلل في التعديل.

---

# إصلاح الأخطاء المكتشفة في الاختبار الميداني بـ4 وكلاء + إعادة اختبار حية

بعد تسليم تقرير الاختبار الميداني (القسم أعلاه)، طلب صاحب المشروع إصلاح كل
الأخطاء الحقيقية المكتشفة، ثم إعادة تشغيل نفس الاختبار بوكلاء جداد للتأكد.

## الإصلاحات المُنفَّذة (كلها اتأكدت حيًا بأرقام حقيقية بعد الإصلاح)

1. **ربط حقول النموذج بـ`<label>`** (`Products.tsx`, `Clients.tsx`): كل حقل
   سعر/تكلفة/كمية/اسم بقى ليه `id`/`htmlFor` سليم. راجعنا كل `value`/`onChange`
   وتأكدنا إنها مربوطة بالـstate الصح من الأساس (مفيش تبديل تسميات فعلي، كان
   بس نقص الربط اللي بيسبب مشاكل مع أي أداة تعبئة آلية وaccessibility).
2. **حركة "وارد" اليدوية بقت بتحدّث متوسط التكلفة فعليًا** (`stock-movements/routes.ts`)
   بنفس صيغة المتوسط المرجّح المستخدمة في فاتورة الشراء. **+ فجوة إضافية
   اكتشفها وكيل إعادة الاختبار**: نموذج "حركة مخزون" في الواجهة ماكانش فيه
   أصلاً حقل لإدخال تكلفة وحدة مختلفة، فالإصلاح الخلفي كان "ميت" عمليًا من
   نفس الشاشة — تمت إضافة حقل "تكلفة الوحدة (اختياري)" في `Products.tsx`
   يظهر فقط لحركة وارد/تعديل-زيادة. اتأكد حيًا: كمية 50 (تكلفة قديمة 80)
   + وارد 50 بتكلفة 100 → متوسط التكلفة الجديد = 90.0000 بالظبط (مطابق
   للصيغة (50×80+50×100)/100).
3. **منتج "خدمة" بقى فعليًا مختلف عن منتج مخزني**: حقول الكمية/حد التنبيه
   وحسابات المخزون/تكلفة البضاعة اختفت من نموذج المنتج لما النوع = خدمة،
   واتصلح استعلام `low_stock` في `reports/routes.ts` (كان بيحسب كل
   المنتجات مش بس المخزنية). **+ اتصلحت كمان أيقونة التحذير الصغيرة جنب اسم
   المنتج في الجدول** اللي كانت بتفضل تظهر لمنتج الخدمة (الأساسية والداشبورد
   كانوا مستثنين الخدمة أصلاً، بس الأيقونة دي في صف الجدول كانت ناسية نفس
   الشرط).
4. **نسبة الضريبة الافتراضية لمنتج جديد** بقت بتُقرأ من إعداد الشركة الفعلي
   (`vat_rate`) بدل رقم قديم ثابت — اتأكد حيًا: شركة نسبتها 14% → منتج جديد
   طلع 14% تلقائيًا (مش القيمة القديمة).
5. **ترجمة التقارير**: أعمار الديون (`current`, `days_1_30`...) والميزانية
   (`current_year_start`, `is_balanced`, نوع الحساب) بقوا عربي بالكامل —
   اتأكد بمراجعة الكود المباشرة (الاستبدال عن طريق `labelMap`/`accountTypeLabel`
   الموجودين أصلاً).
6. **حساب "الخزينة الرئيسية" بقى بيتنشئ تلقائيًا لأي شركة جديدة** أول ما
   تلمس أي شاشة محاسبية (`ensureAccountingSetup`) — اتأكد حيًا إنه شغال
   end-to-end: تحصيل جزئي سجّل فعليًا على "الخزينة الرئيسية" بدون أي إنشاء
   يدوي.
7. **إصلاح جوهري إضافي اكتشفه وكيل إعادة الاختبار (مش في القائمة الأصلية)**:
   إنشاء منتج مخزني بكمية افتتاحية من نموذج "منتج جديد" العادي (المسار
   الأكثر استخدامًا فعليًا) كان بيسيب `average_cost`/`inventory_value` = 0
   بدون أي قيد محاسبي — نفس فئة الخطأ اللي كانت اتصلحت في استيراد CSV بس مش
   في هذا المسار. تم توحيده ليمر عبر نفس `applyOpeningStock()` (INSERT
   بكمية 0 ثم تطبيق الرصيد الافتتاحي بنفس القيد المحاسبي). اتأكد حيًا بعد
   الإصلاح: منتج بكمية 50 وتكلفة 80 → `average_cost: 80`, `inventory_value: 4000`
   (كانوا صفر قبل الإصلاح).
8. **شاشة شطب المخزون**: منتقي المنتج بقى Searchable، وحالة "posted" بقت
   بتترجم لـ"مرحّل" بالعربي.

## ملاحظات لسه مفتوحة (اتلاحظت أثناء إعادة الاختبار، لسه محتاجة قرار/وقت)

- **سباق تحميل صلاحيات الباقة عند فتح رابط عميق مباشر**: فتح رابط زي
  `/app/accounting/reports/trial-balance` مباشرة (بعد تسجيل دخول جديد) بيورّي
  أحيانًا شاشة "هذه الميزة غير مفعّلة في باقتك" وهمية لثوانٍ، بينما نفس
  الصفحة بتفتح عادي لو اتفتحت بالضغط من القائمة الجانبية بعد ما التطبيق
  يكون محمّل بالكامل. يبدو إن `featureSet` في `AppLayout.tsx` مش جاهز وقت ما
  الـguard بيتفعّل على أول تحميل. محتاج مراجعة لاحقًا (انتظار تحميل صلاحيات
  الباقة قبل تفعيل أي حارس صفحة).
- محدد معدل تسجيل الدخول (`authLimiter`) لسه بيتفعّل بسهولة مع استخدام عادي
  عبر عدة جلسات متوازية على نفس الـIP — مذكور تفصيليًا في تقرير الاختبار
  الميداني أعلاه، لسه محتاج قرار هندسي (فصل `/me`/`/refresh` عن حد تسجيل
  الدخول) قبل أي استخدام إنتاجي حقيقي بعدة مستخدمين/أجهزة.

---

# إصلاح: تقارير بتتطبع زي سكرين شوت + سباق تحميل صلاحيات الباقة

بلاغ مباشر من المستخدم: بعض الشاشات لما تتطبع مبتاخدش أي تنسيق طباعة، وكأنها
بتطبع "سكرين شوت" للصفحة كلها بالقائمة الجانبية والأزرار.

**السبب**: النظام فيه أصلاً آلية طباعة نظيفة (`printElementOnly()` في `@/lib/print`
+ خاصية `data-print-area`) بتعزل جزء واحد بس من الصفحة وتطبعه بدون باقي الواجهة
(مستخدمة صح في الفاتورة). لكن 5 شاشات تانية كانت لسه بتستخدم `window.print()`
مباشرة بدون أي عزل: التقارير المالية (`FinancialReportDetail.tsx`)، كشف حساب
العميل (`ClientDetail.tsx`)، كارت الصنف (`ProductDetails.tsx`)، تسوية البنك
(`BankReconciliationDetails.tsx`)، وتقرير "أفضل المنتجات" (`ReportDetail.tsx`).

**تم الإصلاح**: الخمس شاشات دول بقوا يستخدموا نفس نمط `printElementOnly` +
`data-print-area` (مع إضافة عنوان مخصص للطباعة بس `hidden print:block` في كل
شاشة، وإخفاء أزرار/فلاتر التحكم بـ`no-print`). النتيجة: الطباعة دلوقتي بتعزل
محتوى التقرير بس من غير القائمة الجانبية أو الأزرار.

**إصلاح إضافي في نفس الجولة**: لاحظنا كمان إن فتح رابط عميق مباشر (زي
`/app/accounting/reports/trial-balance` بعد تسجيل دخول جديد) بيوري لثوانٍ شاشة
"هذه الميزة غير مفعّلة في باقتك" وهمية قبل ما تختفي. السبب: `useCurrentFeatureSet()`
في `usePlanAccess.ts` بيرجّع `featuresLoaded: false` أثناء انتظار
`/api/subscriptions/features`، لكن `AppLayout.tsx` كان بيفحص `featureSet` فورًا
من غير ما يستنى `featuresLoaded`. تم الإصلاح: الحارس وفلترة القائمة الجانبية
دلوقتي بيستنوا `featuresLoaded` قبل أي قرار حجب — الباك إند برضه بيمنع أي وصول
فعلي غير مصرّح بيه في كل الأحوال، فمفيش أي مخاطرة أمنية من عرض الصفحة أثناء
انتظار التحميل. اتأكد حيًا: فتح رابط ميزان المراجعة مباشرة بعد تسجيل دخول جديد
— مفيش أي ومضة حجب، والصفحة بتحمّل عادي.

---

# إصلاح: منتج/بند فاتورة شراء بتكلفة صفر بيختفي من قائمة الدخل بصمت

بلاغ مباشر من المستخدم: ضاف منتجين (بمورد ومن غيره)، عمل فاتورة شراء ورحّلها،
لكن المنتجين مبيظهروش في قائمة الدخل.

**السبب**: في `postPurchaseInvoice()` بملف `purchases/routes.ts`، بند الفاتورة
(لمنتج نوعه `expense`/`non_stock`/`service` أو بدون منتج) بيتجمّع في
`expenseGroups`، ولو **إجمالي المجموعة = صفر** (يعني تكلفة الوحدة = صفر)، الكود
بيتخطى إضافة أي سطر قيد محاسبي للمصروف خالص (`if (group.amount > 0) ...`). لو
باقي بنود الفاتورة برضه أصفار، الفاتورة كلها بترفض بالكامل برسالة "القيد
المحاسبي الناتج غير متزن أو بقيمة صفرية" (سلوك موجود أصلاً وسليم)، لكن لو فيه
بند تاني بقيمة حقيقية جنبه، الفاتورة بترحّل عادي والبند التاني بس اللي بيختفي
بصمت من التقارير. الجذر: حقل "تكلفة الوحدة" في فاتورة الشراء وحقل "التكلفة" في
نموذج المنتج نفسه الاتنين مسموح يبقوا صفر بدون أي تنبيه.

**تم الإصلاح (تنبيه بدون منع، بطلب صاحب المشروع صراحة — مش رفض صارم عشان
المرونة)**:
- `NewPurchaseInvoice.tsx`: قبل الحفظ، لو أي بند تكلفة وحدته صفر، تنبيه واضح
  (`toast.warning`) باسم البند، من غير ما يمنع الحفظ.
- `Products.tsx`: لو نوع المنتج `stock`/`non_stock`/`expense` (يعني له تكلفة
  بطبيعته، بعكس `service`) والتكلفة صفر، تنبيه مشابه عند الحفظ.

اتأكد بالاختبار الحي: منتج بتكلفة صفر — الحفظ نجح وظهر التنبيه معًا ("تم الحفظ"
+ التحذير). فاتورة شراء ببند وحيد بتكلفة صفر — ظهر نفس التحذير، والفاتورة
رفضت الحفظ (لأنها كانت الوحيدة والإجمالي طلع صفر بالكامل، مش بسبب الإصلاح
الجديد — سلوك موجود أصلاً). لا تعديل على أي schema بالباك إند — النطاق كله
واجهة/تجربة استخدام.

---

# إضافة: كود تلقائي للفرع والموظف (BR-/EM-) وممنوع التعديل

طلب مباشر من صاحب المشروع: كود الفرع وكود الموظف يتولّدوا تلقائيًا عند الإنشاء
(بادئة `BR` للفرع و`EM` للموظف)، ويُمنع المستخدم من تعديلهم بعد كده.

**التنفيذ**: `artifacts/api-server/src/modules/branches/routes.ts` كان
`crudRouter` عام بيقبل `code` من العميل كحقل اختياري قابل للتعديل — استُبدل
براوتر مخصص (نفس أسلوب `employees`/`inventory-write-offs`) بيولّد الكود بنفسه
عبر `nextBranchCode()` بصيغة `BR-00001` تصاعديًا لكل شركة (نفس نمط الترقيم
التسلسلي المستخدم أصلاً في `nextNumber()` لأرقام شطب المخزون)، ومش بيقبل أي
قيمة `code` من العميل خالص — لا في الإنشاء ولا في التعديل (`code` مش موجود في
الـschema أصلاً، فأي قيمة يبعتها العميل بتتجاهل بصمت). نفس المنطق بالظبط
اتطبق على `employee_code` في `artifacts/api-server/src/modules/payroll/routes.ts`
عبر `nextEmployeeCode()` بصيغة `EM-00001`. الواجهتان (`Branches.tsx`,
`Employees.tsx`) اتشالهم حقل إدخال الكود من نموذج الإنشاء بالكامل، وشاشة
تعديل الفرع بقت تعرض الكود كحقل معطّل للقراءة فقط (مفيش شاشة تعديل للموظف
أصلاً).

اتأكد بالاختبار الحي عبر API مباشرة: إنشاء فرع مع محاولة تمرير `code: "HACKED"`
→ اتجاهلت والفرع اتولد بكود `BR-00001` تلقائيًا. محاولة PATCH لنفس الفرع بكود
`"HACKED2"` → الكود فضل `BR-00001` زي ما هو (بس باقي الحقول اتحدثت عادي). فرع
تاني اتولد بعدها بكود `BR-00002` (الترقيم متسلسل صح). نفس الاختبار بالظبط
لموظف جديد بمحاولة تمرير `employee_code: "HACKED"` → اتجاهلت واتولد الكود
`EM-00001` تلقائيًا.

---

# إصلاح: تشغيل الرواتب كان بيحسب الراتب الأساسي كامل بغض النظر عن تاريخ التعيين

بلاغ مباشر من المستخدم: "في حساب الرواتب بيحسب حسب الادخال مش حسب تاريخ
التعيين". بالفحص: `payrollPreview()` في `payroll/routes.ts` كانت بتاخد
`emp.basic_salary` زي ما هو دايمًا لكل موظف نشط، من غير أي اعتبار لـ`hire_date`
مقارنة بالفترة (الشهر/السنة) اللي بيتحسب عنها التشغيل — يعني موظف اتعيّن في
نص الشهر بياخد راتب الشهر كامل، وموظف اتعيّن بعد نهاية الشهر المدفوع عنه
بيظهر برضه في التشغيل وياخد راتب كامل معاه.

**القرار مع صاحب المشروع**: نسبة أيام فعلية (زي أودو/كويك بوكس) — الراتب
المستحق = الأساسي × (عدد أيام العمل الفعلية داخل الشهر ÷ عدد أيام الشهر
الفعلية)، ومنع الموظف من الظهور في التشغيل أصلاً لو تاريخ تعيينه بعد نهاية
الفترة المطلوب تشغيلها.

**التنفيذ**: `payrollPreview()` بقت تحسب `periodStart`/`periodEnd`/`daysInPeriod`
لفترة التشغيل، ولكل موظف: لو `hire_date` بعد نهاية الفترة → يتستبعد بالكامل
من التشغيل، ولو `hire_date` داخل الفترة (بعد بدايتها) → الراتب الأساسي
يتحسب بالتناسب (`basic × daysWorked/daysInPeriod`)، وإلا (اتعيّن قبل الفترة)
الراتب الكامل زي ما كان. الدالة دي مستخدمة في مسار المعاينة (`POST /preview`)
وإنشاء التشغيل الفعلي (`POST /runs`) الاتنين، فالإصلاح بيغطي المعاينة
والترحيل الفعلي مع بعض من نفس المكان. الواجهة (`NewPayrollRun.tsx`) بقت
توضّح تحت رقم الراتب الأساسي لما يكون محسوب بالتناسب: "محسوب بالتناسب: 15
من 30 يوم (تعيين خلال الشهر)" عشان المحاسب يفهم السبب ومايفتكرش إنه خطأ.

**نقطة أثارها المستخدم ولم تُنفَّذ لأنها موجودة فعلاً**: طلب "مساحة للخصم"
لأنه مش شايف مكان للاستقطاعات من الرواتب — الميزة دي موجودة فعليًا من قبل
عبر شاشة "مكونات الرواتب" (`SalaryComponents.tsx`): أي مكون بينشأ بنوع
"استقطاع" بيظهر تلقائيًا كعمود قابل للتعبئة لكل موظف في شاشة "تشغيل رواتب
جديد". المشكلة الأرجح كانت عدم إنشاء أي مكون استقطاع بعد، مش غياب الميزة
نفسها — يستحق توضيح للمستخدم بدل بناء آلية مكرّرة.

اتأكد بالاختبار الحي عبر API: موظف براتب 3,000 اتعيّن 2026-09-16، تشغيل
لشهر سبتمبر 2026 (30 يوم) → الراتب المحسوب 1,500.00 بالظبط (=3000×15/30،
15 يوم فعلي من 16 لـ30 شامل الطرفين). موظف اتعيّن 2026-10-05 وتشغيل لشهر
سبتمبر → اختفى تمامًا من نتيجة المعاينة. موظف قديم (تعيين 2020) → راتبه
الكامل زي ما هو من غير أي تغيير.

**تعديلان إضافيان لنفس الشاشة/الميزة بطلب المستخدم**:
- **حقلا "الشهر"/"السنة" في شاشة "تشغيل رواتب جديد" ملهمش أي `<Label>`
  خالص** (بلاغ المستخدم: "شايف inputs مش مفهومة على التابلت") — بعكس كل
  حقول الإدخال التانية في النظام. تمت إضافة `<Label>الشهر</Label>` و
  `<Label>السنة</Label>` فوق الحقلين في `NewPayrollRun.tsx`.
- **الراتب الأساسي بقى إجباري ولا يقبل صفر**: الفرونت إند (`Employees.tsx`)
  بيرفض الحفظ برسالة واضحة لو الراتب فاضي أو صفر، والباك إند
  (`employeeSchema.basic_salary`) اتغيّر من `.nonnegative()` لـ`.positive()`
  كخط دفاع تاني. اتأكد حيًا: محاولة إنشاء موظف براتب 0 عبر API مباشر
  رجّعت خطأ validation واضح ("Number must be greater than 0")، وموظف براتب
  100 اتحفظ عادي.
- **حساب الرواتب (الأساسي المحسوب بالتناسب + الإجماليات + الصافي) بقى
  برقم صحيح بدون كسور** (طلب المستخدم: "خلي حساب الرواتب برقم تقريبي بدون
  كسور") — بدل التقريب لخانتين عشريتين، بقى تقريب لأقرب رقم صحيح
  (`roundWhole` بدل `round2` في `payrollPreview()`). اتأكد حيًا: موظف براتب
  3,000 اتعيّن 2026-08-15 وتشغيل لشهر أغسطس (31 يوم، 17 يوم فعلي) → الناتج
  الرياضي الدقيق 1,645.161290... ج.م، ظهر في الشاشة كـ**1,645** بالظبط
  (صحيح بدون كسور).

---

# إصلاح: تغييرات الواجهة (نص/تصميم) مش بتظهر للمستخدم حتى بعد تفريغ كاش Cloudflare

بلاغ مباشر من المستخدم: تعديلات على نصوص/تصميم الواجهة مش بتظهر له، وفضلت المشكلة
موجودة حتى بعد ما فرّغ كاش Cloudflare بنفسه — يعني السبب مش كاش الـCDN.

**السبب الحقيقي**: في `artifacts/api-server/src/app.ts` (سطر ~175)، السيرفر في وضع
الإنتاج بيقدّم بناء الواجهة المبني (`dist/public`) عن طريق
`express.static(FRONTEND_DIST, { maxAge: '1d', index: 'index.html' })` — الخيار
`maxAge: '1d'` ده بيتطبّق على **كل** ملف بيتقدّم من المجلد ده **بما فيهم
`index.html` نفسه**. لكن `index.html` هو اللي بيحتوي على أسماء ملفات الـJS/CSS
المبنية بكود hash فريد لكل نسخة بناء (زي `index-BGjjjfXk.js`) — فلما المتصفح
(أو أي طبقة كاش زي Cloudflare لو مفعّل عليها "Cache Everything") يخزّن نسخة
`index.html` القديمة لمدة يوم كامل، فضل بيحمّل نفس ملفات JS/CSS القديمة حتى
بعد نشر تعديل جديد فعليًا على السيرفر — وده بالظبط ليه تفريغ كاش Cloudflare
لوحده ملوش تأثير: النسخة المخزّنة في **متصفح المستخدم نفسه** (browser cache)
هي المشكلة، مش كاش الـCDN.

**تم الإصلاح**: فصلت سياسة الكاش حسب نوع الملف بدل قيمة واحدة للكل:
- `index.html` (وأي ملف تاني غير مُهشّش زي `favicon.ico`/`robots.txt`) →
  `Cache-Control: no-cache` — المتصفح/الـCDN لازم يتأكد من السيرفر في كل مرة
  إن النسخة لسه أحدث نسخة قبل ما يستخدمها من الكاش المحلي.
- ملفات `/assets/*` المُهشّشة (JS/CSS/صور، أسماؤها بتتغيّر تلقائيًا مع أي
  تعديل في المحتوى بفضل Vite) → `Cache-Control: public, max-age=31536000, immutable`
  — تخزين دائم آمن 100% لأن أي تغيير حقيقي في المحتوى بيولّد اسم ملف مختلف
  خالص، فمفيش أي احتمال لتضارب نسخة قديمة/جديدة.

اتأكد بمحاكاة السيرفر فعليًا (نفس منطق `express.static` بالظبط على نسخة بناء
حقيقية موجودة): طلب `/` وأي مسار SPA تاني والملفات غير المُهشّشة (`favicon.ico`)
رجّعوا `Cache-Control: no-cache`، وملف `/assets/index-BGjjjfXk.js` المُهشّش
رجّع `Cache-Control: public, max-age=31536000, immutable` بالظبط.

**ملحوظة**: النسخة القديمة اللي كانت مخزّنة في متصفحات المستخدمين الحاليين
هتفضل موجودة لحد ما تنتهي مدتها الطبيعية (يوم واحد بحد أقصى) أو المستخدم يعمل
تحديث قسري (Ctrl+Shift+R) — بعد نشر هذا الإصلاح، أي نشر جديد بعد كده هيوصل
فورًا من غير أي مشكلة مشابهة.

---

# مراجعة قسم "سوبر أدمن" (إدارة منصة الـSaaS نفسها)

نفس منهجية المراجعات السابقة (مبيعات/مخزون/محاسبة/رواتب/إعدادات)، لكن على
طبقة تشغيل المنصة نفسها بدل حسابات الشركات: `platform/routes.ts`،
`plans/routes.ts` (`adminPlansRouter`)، `subscriptions/routes.ts`،
`middleware/rbac.ts`/`tenant.ts`، وشاشات `pages/admin/*.tsx`.

**نقطة إيجابية عامة**: التحقق من صلاحية Super Admin (`is_super_admin` عمود
منفصل على `users`، وليس دور نصّي) مطبَّق مركزيًا (`router.use(requireSuperAdmin)`)
على كل مسارات المنصة — مفيش أي endpoint إداري لقيته بدون تحقق فعلي، ومفيش أي
اعتماد خاطئ على `tenantContext`/عزل تلقائي بين الشركات (كل استعلام في
`platform/routes.ts` بيستخدم `company_id` صريح).

## أخطاء حقيقية (تم إصلاحهما)

1. ~~حذف "طلب تسجيل" معلّق (`DELETE /api/platform/signups/:id`) كان بيتحقق
   من جدول `invoices` بس قبل حذف الشركة بالكامل (Cascade على عشرات
   الجداول)~~ — **تم الإصلاح**. المسار `PATCH /signups/:id/reset` بيرجّع أي
   شركة لحالة "قيد المراجعة" بدون أي شرط، حتى لو كانت شغالة فعليًا وعندها
   عملاء/قيود/فواتير شراء (فقط لو ملهاش فواتير *بيع*، الفحص الوحيد اللي كان
   موجود). النتيجة: زرار "حذف الطلب نهائيًا" (اللي نصه بيوحي إنه بيمسح مجرد
   طلب فاضي) كان ممكن يمسح شركة حقيقية بكل بياناتها نهائيًا وبلا رجعة. تم
   استبدال الفحص بدالة `companyHasOperationalData()` (`platform/routes.ts`)
   بتفحص **كل** جدول عنده عمود `company_id` (بتكتشفها ديناميكيًا من
   `information_schema.columns` بدل قايمة ثابتة، عشان أي جدول جديد يتضاف
   مستقبلاً يتغطى تلقائيًا) ما عدا جداول إدارية بحتة بتتعمر تلقائيًا زي
   `subscriptions`/`user_companies`/`accounting_settings` (مفيش داعي تمنع
   الحذف بسببها). التحقق اتعمل بمراجعة الكود + `tsc --noEmit` نظيف (مفيش
   قاعدة بيانات متاحة في هذه الجلسة لاختبار حي كامل end-to-end).
2. ~~تعطيل مستخدم من شركة (`PATCH /api/users/:id/status`) بيقطع وصوله عن كل
   الشركات التانية اللي هو عضو فيها~~ — **تم الإصلاح**. عمود `users.disabled`
   عالمي على مستوى الحساب (بيتفحص في تسجيل الدخول قبل أي سياق شركة)، لكن
   endpoint التعطيل كان بيتحقق بس من عضوية المستخدم في شركة المدير الحالي —
   فمدير شركة A (بدون أي صلاحية على شركة B) كان يقدر يعطّل حساب مستخدم مشترك
   بين الشركتين، ويقطع دخوله عن شركة B كمان بدون علم مديرها. تم إضافة تحقق:
   لو المستخدم عضو في شركة تانية غير شركة المدير الحالي، طلب التعطيل (مش
   التفعيل) بيترفض برسالة توضح إن الإجراء الصح هو "إزالة المستخدم من هذه
   الشركة" (`DELETE /:id` الموجود، وده بيشيل العضوية/الدور الخاص بشركة واحدة
   بس، سلوك صحيح من الأساس). حل جذري كامل (تعطيل لكل شركة على حدة بدل عمود
   عالمي) كان هيحتاج تعديل نموذج البيانات ومنطق تسجيل الدخول نفسه — أُجّل
   عمدًا كتغيير أكبر يحتاج قرار منفصل، والإصلاح الحالي يمنع السيناريو الخطير
   (قفل حساب في شركة تانية بدون علم صاحبها) فورًا.

## فجوات ميزات (ليست أخطاء)

- لا يوجد تدقيق (`audit_log`) لتعديل الأدوار المخصصة (`PATCH /custom-roles/:id`)
  رغم إن الإنشاء/الحذف مسجَّلين — نفس فئة عدم اتساق التغطية المذكورة في
  مراجعة الإعدادات.
- لا يوجد زرار تعطيل/تفعيل حساب مستخدم من لوحة Super Admin نفسها (موجود على
  مستوى الشركة بس).
- لا يوجد "انتحال شخصية" (impersonate) لمستخدم/شركة من لوحة Super Admin
  للدعم الفني.
- لا يوجد سجل نسخ سابقة (undo/history) لتغييرات مصفوفة صلاحيات الباقات
  (`feature_access`) أو الأدوار المخصصة عند تعديل جماعي.
- لا يوجد Rate limiting أو 2FA خاص بحسابات Super Admin (نفس محدد المعدل
  العام المذكور كفجوة في مراجعة الإعدادات).

## نقاط تصميم مقصودة (ليست أخطاء)

- دور `super_admin` كقيمة نصّية داخل `user_roles.role` موجود في الـschema لكن
  **معطَّل عمليًا بالكامل** — الصلاحية الفعلية بتُحسم فقط من `users.is_super_admin`،
  والقيمة النصية مش مقروءة في أي مكان تاني ولا معروضة في أي قائمة اختيار
  بالواجهة. قيمة ميتة في الـschema تستحق تنظيف لاحق لكنها مش ثغرة.
- "الأدوار المخصصة" في `RolesAndPermissions.tsx` مرجعية فقط ولا تُطبَّق على
  أي endpoint فعليًا — موضّح صراحةً في نص الواجهة نفسها، قرار معلن ومتسق مع
  فجوة "الأدوار ثابتة بدون صلاحيات مخصصة" الموثقة سابقًا.
- `adminPlansRouter` مُركَّب داخل مجموعة `/api` المصادَق عليها (مش بجانب
  `publicPlansRouter`) — إصلاح سابق متعمد وموثّق في تعليق أعلى الملف عشان
  `requireSuperAdmin` يقدر يشوف `req.tenant.isSuperAdmin`.
- ازدواجية منطق "تغيير باقة الشركة" بين `platform/routes.ts` (بمفتاح
  `company_id`، تستخدمه `Companies.tsx`) و`subscriptions/routes.ts` (بمفتاح
  `subscription_id`، تستخدمه `Subscriptions.tsx`) — تكرار كود (technical debt)
  مش خطأ سلوكي، الفرق الوحيد (عدم تحقق من `plan.is_active` في النسخة التانية)
  مش قابل للوصول عمليًا لأن الواجهة بتصفّي الباقات غير الفعّالة قبل الإرسال أصلاً.
- `requireActiveSubscription` بيتخطى Super Admin بشكل صريح رغم إنه مُركَّب
  على كل `/api/*` بما فيها `/api/platform` — تصميم صحيح ومقصود (منعًا لقفل
  الأدمن بسبب اشتراك شركته هو نفسه).
